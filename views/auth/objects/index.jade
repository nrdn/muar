extends ../../layout

block title
	title СПИСОК ОБЪЕКТОВ

block content_style
	link(rel='stylesheet', type='text/css', href='/build/css/add.css')

block content_scripts
	script(type='text/javascript', src='/build/js/lists.js')

block content
	a.back_link(href='/auth') < Назад
	.title СПИСОК ОБЪЕКТОВ
	br
	a.add(href='/auth/objects/add') добавить объект
	br
	br
	.ages_navigate_block
		.ages_all Все
		each age in ages
			.age_navigate_block
				.age_navigate_title(id=age._id)= age.i18n.title.get('ru')
				.ages_sub_list
					.sub_navigate_block
						each sub in age.sub
							.sub_item(id=sub._id)= sub.i18n.title.get('ru')

	.main_menu_block
		each object in objects
			.object(class=object.ages.main, class=object.ages.sub)
				a.rm_object(href='#', id=object._id)= 'удалить '
				!=' | '
				a(href='/auth/objects/edit/' + object._id)= object.i18n.title.get('ru')
				!=' --> '
				a(href='/auth/objects/' + object._id + '/subjects') единицы хранения
				span.object_options
					if object.ages.sub.length == 0
						span.option.not_ages 🚎
					if object.images.length > 6
						span.option.not_images 🗽